Things we need to do:

1. Tokenize file by 'parts of speech' (more like by line) [check] 
	a. Targets [check]
	b. Dependencies
		- Map dependency rules to targets
		- Create Queue of rules from dependencies in correct order
	c. Commands/Rules [check]
2. Don't die [so far so good, check]
